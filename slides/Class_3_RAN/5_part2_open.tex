%!TEX root = ./main.tex

% SECTION TIMING: 0:00--8:00 (2 frames). Two days have passed and students remember
% less than you think. Re-establish the rule and the loose end before anything else --
% both of Wednesday's payoffs depend on Monday's setup being fresh.

\begin{frame}[plain]
  \vspace{1.4em}
  \centering
  {\Large\textcolor{softgray}{Part 2}\par}
  \vspace{0.7em}
  {\Huge\textbf{How the network became software}\par}
  \vspace{1.2em}
  {\large Wednesday, 9/2\par}
  \vspace{2.0em}
  {\normalsize\textcolor{softgray}{5G core \quad$\cdot$\quad Security \quad$\cdot$\quad
   O-RAN \quad$\cdot$\quad Who pays for it}\par}
\end{frame}

\begin{frame}{Two things from Monday you need today}
  \vspace{0.5em}
  \begin{columns}[T,onlytextwidth]
    \column{0.47\textwidth}
      {\small\textbf{\textcolor{ranblue}{The rule}}\par}

      \vspace{0.5em}
      \small
      One box becomes two when its halves need to change at different speeds.

      \vspace{0.6em}
      You saw it twice: the 2G base station split into radio and control, and the
      GPRS core split into decisions and packets.

      \vspace{0.6em}
      \textbf{Today it happens twice more.}
    \column{0.47\textwidth}
      {\small\textbf{\textcolor{coreorange}{The loose end}}\par}

      \vspace{0.5em}
      \small
      Your phone attaches to the strongest signal it can hear.

      \vspace{0.6em}
      In 2G it proved itself to the network, and the network proved nothing back.

      \vspace{0.6em}
      \textbf{Today we find out what four generations did about that.}
  \end{columns}

  \vspace{0.9em}
  \glossline{RAN = Radio Access Network \quad Core = everything behind the towers}
\end{frame}
