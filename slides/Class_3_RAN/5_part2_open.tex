%!TEX root = ./main.tex

\section[5G Core]{The Core Becomes Software}

% SECTION TIMING: 0:00--6:00 (2 frames). Two days have passed and students remember
% less than you think. Re-establish the rule and the loose end before anything else --
% both of Wednesday's payoffs depend on Monday's setup being fresh.

\begin{frame}[plain]
  \vspace{1.4em}
  \centering
  {\Large\textcolor{softgray}{Part 2}\par}
  \vspace{0.7em}
  {\Huge\textbf{How the network became software}\par}
  \vspace{1.2em}
  {\large Wednesday, 9/2\par}
  \vspace{2.0em}
  {\normalsize\textcolor{softgray}{5G core \quad$\cdot$\quad Security \quad$\cdot$\quad
   O-RAN \quad$\cdot$\quad Who pays for it}\par}
\end{frame}

\begin{frame}{Two things from Monday you need today}
  \vspace{0.4em}
  \begin{columns}[T,onlytextwidth]
    \column{0.47\textwidth}
      {\small\textbf{\textcolor{ranblue}{The rule}}\par}

      \vspace{0.4em}
      \small
      One box becomes two along the seam between \textbf{deciding} and
      \textbf{carrying} --- because the two halves grow at different rates.

      \vspace{0.5em}
      You saw it three times: the 2G base station, the MSC in 2001, and the
      GPRS core.

      \vspace{0.5em}
      \textbf{Today it happens twice more} --- and the second one bends the rule.
    \column{0.47\textwidth}
      {\small\textbf{\textcolor{coreorange}{The loose end}}\par}

      \vspace{0.4em}
      \small
      Your phone attaches to the strongest signal it can hear.

      \vspace{0.5em}
      In 2G it proved itself to the network, the network proved nothing back
      --- and a tower that \textbf{insisted} could make the phone hand over its
      permanent name.

      \vspace{0.5em}
      \textbf{Equipment built to insist is called an IMSI catcher.}
  \end{columns}

  \vspace{0.7em}
  \ask{On Monday you wrote down one box that disappeared, and what pressure
    removed it. Which one did you pick?}
\end{frame}
